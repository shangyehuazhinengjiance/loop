# 任务拆解

1. **UI/UX：聊天界面与默认头像规范设计** (todo) — @human-ab44e6c8
   参考微信聊天界面，输出气泡样式、左右对齐排版、颜色规范及时间戳显示逻辑的设计图。同时确定默认头像的视觉规范（如图标样式或首字母生成规则）。

2. **前端：通用头像组件与默认头像开发** (todo) — @human-ab44e6c8
   开发一个通用的 Avatar 组件，支持传入头像 URL。当 URL 为空时，按照设计规范展示默认头像（默认图标或首字母）。

3. **前端：聊天气泡与基础排版重构** (todo) — @human-ab44e6c8
   实现消息的左右分栏布局（当前用户在右，其他用户/AI在左），引入新开发的 Avatar 组件，并应用不同的气泡背景色（自己为绿色，他人为白色/浅色）。

4. **前端：细节优化（时间戳、昵称与间距）** (todo) — @human-ab44e6c8
   实现消息间距调整、超过5分钟的时间戳居中显示逻辑，以及在左侧消息上方显示发送者昵称。

5. **前端：Markdown 与富文本气泡兼容性处理** (todo) — @human-ab44e6c8
   确保 Markdown 内容（如代码块、列表、长链接）在新的气泡样式下能够正常换行和渲染，代码块支持内部横向滚动，防止撑破气泡布局。