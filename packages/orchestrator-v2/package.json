{
  "name": "@loop/orchestrator-v2",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 3000",
    "migrate": "python -m app.migrate",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 3000"
  }
}
