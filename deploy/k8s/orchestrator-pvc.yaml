apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loop-workspaces
  namespace: loop
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  # storageClassName: your-storage-class   # 按公司集群填写
