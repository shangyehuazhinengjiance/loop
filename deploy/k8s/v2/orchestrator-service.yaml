apiVersion: v1
kind: Service
metadata:
  name: loop-orchestrator
  namespace: loop
  labels:
    app: loop-orchestrator
spec:
  selector:
    app: loop-orchestrator
  ports:
    - name: http
      port: 3000
      targetPort: 3000
